services:
  portainer-ce:
    ports:
      - 8000:8000 
      - 9000:9000 # http
      - 9443:9443 # https
    container_name: portainer
    restart: always
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./portainer_data:/data
    image: portainer/portainer-ce:lts
